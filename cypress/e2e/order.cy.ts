describe('order modal works as expected', () => {
	beforeEach(() => {
		// cy.intercept('GET', 'api/ingredients', { fixture: 'ingredients.json' }).as('getIngredientsResponse');
		// cy.intercept('POST', 'api/orders', { fixture: 'order.json' }).as('createOrderResponse');
		// cy.intercept('GET', 'api/auth/user', { fixture: 'user.json' }).as('getUserResponse');
		// cy.intercept('PATCH', 'api/auth/user', { fixture: 'user.json' }).as('updateUserResponse');
		// cy.intercept('GET', 'api/orders', { fixture: 'orders.json' }).as('getOrdersResponse');
		// cy.intercept('GET', 'api/orders/all', { fixture: 'feeds.json' }).as('getFeedsResponse');
		// cy.visit('http://localhost:4000/');
	});
});
